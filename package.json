{
  "name": "student-budget-pwa",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"Run per workspace: npm run -w @app/backend dev && npm run -w @app/frontend dev\"",
    "build": "echo \"Run per workspace: npm run -w @app/backend build && npm run -w @app/frontend build\"",
    "lint": "eslint .",
    "format": "prettier --write .",
    "typecheck": "tsc -v"
  },
  "devDependencies": {
    "eslint": "^9.11.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
